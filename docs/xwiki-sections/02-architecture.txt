= (% id="H2" %)🏗️ 2. 시스템 아키텍처 =

{{panel title="**✨ 이 섹션에서 배울 내용**"}}
* ☑️ 5개 마이크로서비스 구성 및 역할
* ☑️ 3단계 차단 로직 (화이트리스트 우선)
* ☑️ 데이터베이스 스키마 (ERD)
* ☑️ REGTECH + SECUDIUM 자동 수집
* ☑️ Redis 캐싱 전략 및 보안 레이어
{{/panel}}

---

== 1️⃣ 시스템 개요 ==

=== 🎯 핵심 원칙 ===

{{info}}
**🥇 최우선 원칙**

(% style="font-size:18px; color:#4CAF50; font-weight:bold;" %)
화이트리스트가 항상 블랙리스트보다 우선합니다
(%)

→ 화이트리스트 IP는 블랙리스트에 있어도 **절대 차단되지 않습니다**
{{/info}}

=== 🐳 5개 컨테이너 구성 ===

(% class="wikitable" %)
|=(% style="background:#37474F; color:white;" %)아이콘|=(% style="background:#37474F; color:white;" %)컨테이너|=(% style="background:#37474F; color:white;" %)역할|=(% style="background:#37474F; color:white;" %)포트|=(% style="background:#37474F; color:white;" %)기술 스택
|⚛️ |blacklist-frontend|웹 인터페이스|2543 (내부)|Next.js 14 + Ant Design
|🐍|blacklist-app|REST API|2542 (내부)|Flask 3.0 + Python 3.11
|📊|blacklist-collector|데이터 수집 (2개 소스)|8545 (내부)|Python 3.11 + pandas
|🐘|blacklist-postgres|데이터베이스|5432 (내부)|PostgreSQL 15 + JSONB
|🔴|blacklist-redis|캐시 + Rate Limit|6379 (내부)|Redis 7

{{info}}
**🆕 최신 개선사항 (v3.3.7 - 2025-10-29)**
* **Traefik 연동**: 별도 설치 필요, 역방향 프록시 및 SSL 처리
* **도메인**: blacklist.nxtd.co.kr
* **이중 수집**: REGTECH + SECUDIUM 동시 지원
* **보안 강화**: CSRF 보호, Rate Limiting (Flask-Limiter)
* **컨테이너**: 5개 서비스 (nginx 제거)
{{/info}}

**🏛️ 시스템 아키텍처:**

{{plantuml}}
@startuml
skinparam componentStyle rectangle
skinparam backgroundColor white
skinparam component {
    BackgroundColor<<presentation>> LightBlue
    BackgroundColor<<application>> LightGreen
    BackgroundColor<<data>> LightYellow
    BackgroundColor<<external>> LightCoral
}

rectangle "사용자 계층" {
    actor "웹 브라우저" as USER
    actor "FortiGate/WAF" as CLIENT
}

package "리버스 프록시" <<external>> {
    component "Traefik\n(별도 설치 필요)" as TRAEFIK
}

package "프레젠테이션 계층" <<presentation>> {
    component "blacklist-frontend\n(Next.js :2543)" as FRONTEND
}

package "애플리케이션 계층" <<application>> {
    component "blacklist-app\n(Flask :2542)" as APP
    component "blacklist-collector\n(수집기 :8545)" as COLLECTOR
}

package "데이터 계층" <<data>> {
    database "PostgreSQL\n(:5432)" as DB
    database "Redis\n(:6379)" as REDIS
}

cloud "외부 시스템" {
    component "REGTECH" as REGTECH
    component "SECUDIUM" as SECUDIUM
}

USER --> TRAEFIK : HTTPS
CLIENT --> TRAEFIK : HTTPS
TRAEFIK --> FRONTEND : HTTP (내부)
FRONTEND --> APP
APP --> DB
APP --> REDIS
COLLECTOR --> REGTECH
COLLECTOR --> SECUDIUM
COLLECTOR --> DB

@enduml
{{/plantuml}}

{{warning}}
**⚠️ Traefik 설정 필요**
* Traefik은 별도로 설치 및 설정해야 합니다
* 설정 가이드: {{code}}docs/TRAEFIK-SETUP.md{{/code}}
* 위치: {{code}}/home/jclee/infrastructure/traefik/{{/code}}
{{/warning}}

---

== 2️⃣ 차단 로직 ==

=== 🎯 3단계 의사결정 프로세스 ===

{{panel title="**🔍 의사결정 우선순위**"}}
1️⃣ **화이트리스트 체크** (최우선) → 화이트리스트 보호
2️⃣ **블랙리스트 체크** → 위협 IP 차단
3️⃣ **정상 허용** → 기본값 (차단하지 않음)
{{/panel}}

**📊 의사결정 흐름도:**

{{plantuml}}
@startuml
start
:IP 조회 요청;

if (화이트리스트 존재?) then (yes)
  :✅ 허용 (whitelist);
  stop
else (no)
  if (블랙리스트 존재?) then (yes)
    :❌ 차단 (blacklisted);
    stop
  else (no)
    :✅ 허용 (not_found);
    stop
  endif
endif

@enduml
{{/plantuml}}

=== 💻 코드 구현 (Flask) ===

{{code language="python"}}
# app/core/services/blacklist_service.py:~120
def check_blacklist(ip: str):
    # Step 1: 화이트리스트 체크 (최우선)
    if self.is_whitelisted(ip):
        logger.info("whitelist_hit", ip=ip)
        return {"blocked": False, "reason": "whitelist"}

    # Step 2: 블랙리스트 체크
    if ip in blacklist_data:
        return {"blocked": True, "reason": "blacklisted"}

    # Step 3: 기본 허용
    return {"blocked": False, "reason": "not_found"}
{{/code}}

{{success}}
**✅ 핵심 비즈니스 규칙**
* VIP 고객 IP는 **무조건 허용** (화이트리스트 우선)
* 블랙리스트는 화이트리스트가 아닌 경우에만 적용
* 미등록 IP는 기본적으로 허용 (차단하지 않음)
{{/success}}

---

== 3️⃣ 데이터베이스 스키마 ==

=== 📊 ERD (Entity Relationship Diagram) ===

{{plantuml}}
@startuml
entity "whitelist_ips" {
  * id : SERIAL <<PK>>
  --
  * ip_address : VARCHAR(45) <<UNIQUE>>
  * country : VARCHAR(2)
  * is_active : BOOLEAN
  * reason : TEXT
  * created_at : TIMESTAMP
  * updated_at : TIMESTAMP
}

entity "blacklist_ips" {
  * id : SERIAL <<PK>>
  --
  * ip_address : VARCHAR(45) <<UNIQUE>>
  * country : VARCHAR(2)
  * notes : TEXT
  raw_data : JSONB
  * created_at : TIMESTAMP
  * updated_at : TIMESTAMP
}

entity "collection_credentials" {
  * id : SERIAL <<PK>>
  --
  source_type : VARCHAR(50)
  * username_encrypted : BYTEA
  * password_encrypted : BYTEA
  is_active : BOOLEAN
  * created_at : TIMESTAMP
  * updated_at : TIMESTAMP
}

entity "collection_stats" {
  * id : SERIAL <<PK>>
  --
  * source_type : VARCHAR(50)
  * collection_date : TIMESTAMP
  total_ips : INTEGER
  new_ips : INTEGER
  updated_ips : INTEGER
  status : VARCHAR(20)
  error_message : TEXT
}

entity "session_history" {
  * id : SERIAL <<PK>>
  --
  * session_id : UUID
  * ip_address : VARCHAR(45)
  country : VARCHAR(2)
  * status : VARCHAR(20)
  risk_level : VARCHAR(20)
  * timestamp : TIMESTAMP
}

whitelist_ips }o--|| collection_credentials : "수집 인증"
blacklist_ips }o--|| collection_credentials : "수집 인증"
collection_stats }o--|| collection_credentials : "수집 통계"
session_history }o--|| blacklist_ips : "차단 이력"
@enduml
{{/plantuml}}

=== 📋 주요 테이블 설명 ===

==== 1. whitelist_ips (화이트리스트) ====

{{info}}
**목적**: VIP 고객 IP 보호 (최우선 허용)
{{/info}}

(% class="wikitable" %)
|=(% style="background:#4CAF50; color:white;" %)컬럼|=(% style="background:#4CAF50; color:white;" %)타입|=(% style="background:#4CAF50; color:white;" %)설명
|{{code}}id{{/code}}|SERIAL|자동 증가 ID (PK)
|{{code}}ip_address{{/code}}|VARCHAR(45)|IP 주소 (UNIQUE, IPv4/IPv6)
|{{code}}country{{/code}}|VARCHAR(2)|국가 코드 (ISO 3166-1 alpha-2)
|{{code}}is_active{{/code}}|BOOLEAN|활성화 상태 (TRUE=활성, FALSE=비활성)
|{{code}}reason{{/code}}|TEXT|화이트리스트 등록 사유
|{{code}}created_at{{/code}}|TIMESTAMP|등록 시각
|{{code}}updated_at{{/code}}|TIMESTAMP|수정 시각

==== 2. blacklist_ips (블랙리스트) ====

{{info}}
**목적**: 위협 IP 차단 (화이트리스트 제외)
{{/info}}

(% class="wikitable" %)
|=(% style="background:#F44336; color:white;" %)컬럼|=(% style="background:#F44336; color:white;" %)타입|=(% style="background:#F44336; color:white;" %)설명
|{{code}}id{{/code}}|SERIAL|자동 증가 ID (PK)
|{{code}}ip_address{{/code}}|VARCHAR(45)|IP 주소 (UNIQUE)
|{{code}}country{{/code}}|VARCHAR(2)|국가 코드
|{{code}}notes{{/code}}|TEXT|차단 사유 및 메모
|{{code}}raw_data{{/code}}|JSONB|REGTECH/SECUDIUM 원본 데이터
|{{code}}created_at{{/code}}|TIMESTAMP|등록 시각
|{{code}}updated_at{{/code}}|TIMESTAMP|수정 시각

==== 3. collection_credentials (수집 인증) ====

{{info}}
**목적**: REGTECH + SECUDIUM 인증정보 암호화 저장
{{/info}}

(% class="wikitable" %)
|=(% style="background:#607D8B; color:white;" %)컬럼|=(% style="background:#607D8B; color:white;" %)타입|=(% style="background:#607D8B; color:white;" %)설명
|{{code}}source_type{{/code}}|VARCHAR(50)|수집 소스 (REGTECH, SECUDIUM)
|{{code}}username_encrypted{{/code}}|BYTEA|AES-256 암호화된 사용자명
|{{code}}password_encrypted{{/code}}|BYTEA|AES-256 암호화된 비밀번호
|{{code}}is_active{{/code}}|BOOLEAN|활성화 상태

---

== 4️⃣ 데이터 수집 메커니즘 ==

=== 📊 이중 소스 수집 ===

{{panel title="**🔄 지원 소스**"}}
1️⃣ **REGTECH**: 한국인터넷진흥원 (KISA) 위협정보
2️⃣ **SECUDIUM**: 글로벌 위협 인텔리전스
{{/panel}}

**🔁 수집 흐름:**

{{plantuml}}
@startuml
participant "스케줄러" as SCHED
participant "Collector" as COLL
participant "REGTECH API" as REGTECH
participant "SECUDIUM API" as SECUDIUM
database "PostgreSQL" as DB

SCHED -> COLL : 일일 수집 트리거 (86400초)
activate COLL

group REGTECH 수집
COLL -> REGTECH : 인증 (findOneMember)
REGTECH --> COLL : 세션 토큰
COLL -> REGTECH : 로그인 (addLogin)
REGTECH --> COLL : 인증 완료
COLL -> REGTECH : Excel 다운로드
REGTECH --> COLL : IP 목록 (xlsx)
end

group SECUDIUM 수집
COLL -> SECUDIUM : 토큰 인증
SECUDIUM --> COLL : Access Token
COLL -> SECUDIUM : Excel 다운로드
SECUDIUM --> COLL : IP 목록 (xlsx)
end

COLL -> DB : IP 목록 저장 (blacklist_ips)
COLL -> DB : 수집 통계 저장 (collection_stats)

deactivate COLL
@enduml
{{/plantuml}}

=== ⏰ 자동 스케줄링 ===

{{code language="python"}}
# collector/monitoring_scheduler.py
COLLECTION_INTERVAL = 86400  # 24시간 (초 단위)

# cron 표현식 (대안)
# 0 2 * * * - 매일 새벽 2시
{{/code}}

{{warning}}
**⚠️ 중요**
* REGTECH는 2단계 인증 (findOneMember → addLogin)
* SECUDIUM는 토큰 기반 인증
* Excel 파일은 pandas + openpyxl로 파싱
* 수집 실패 시 자동 재시도 (3회)
{{/warning}}

---

== 5️⃣ Redis 캐싱 전략 ==

=== 🚀 성능 최적화 ===

{{panel title="**💾 캐시 사용 목적**"}}
* IP 조회 속도 향상 (DB → Redis)
* Rate Limiting 백엔드
* Session 저장
{{/panel}}

**📊 캐시 키 구조:**

{{code language="text"}}
# IP 캐시 (화이트리스트 + 블랙리스트)
whitelist:192.168.1.1 → {"blocked": false, "reason": "whitelist"}
blacklist:1.2.3.4 → {"blocked": true, "reason": "blacklisted"}

# Rate Limit (Flask-Limiter)
rate-limit:192.168.1.1 → {count: 45, ttl: 3600}

# TTL (Time To Live)
- IP 캐시: 1시간 (3600초)
- Rate Limit: 요청당 설정 (기본 1시간)
{{/code}}

=== 🔄 캐시 갱신 전략 ===

{{plantuml}}
@startuml
start
:IP 조회 요청;

if (Redis에 캐시 존재?) then (yes)
  :캐시 반환 (빠름);
  stop
else (no)
  :DB 조회;
  :Redis 캐시 저장 (TTL 1시간);
  :결과 반환;
  stop
endif
@enduml
{{/plantuml}}

---

== 6️⃣ 보안 아키텍처 ==

=== 🔒 다층 보안 레이어 ===

{{panel title="**🛡️ Defense-in-Depth**"}}
1️⃣ **CSRF 보호** (Flask-WTF)
2️⃣ **Rate Limiting** (Redis 기반)
3️⃣ **보안 헤더** (X-Frame-Options, CSP)
4️⃣ **Input Validation** (IP 형식, SQL Injection 방지)
{{/panel}}

**🔐 보안 레이어:**

{{code language="python"}}
# app/core/app.py::create_app()

# Layer 1: CSRF Protection
csrf = CSRFProtect()
csrf.init_app(app)

# Layer 2: Rate Limiting
limiter = Limiter(
    app=app,
    key_func=get_remote_address,
    storage_uri=f"redis://{REDIS_HOST}:{REDIS_PORT}",
    default_limits=["200/day", "50/hour"]
)

# Layer 3: Security Headers
@app.after_request
def set_security_headers(response):
    response.headers['X-Frame-Options'] = 'DENY'
    response.headers['Content-Security-Policy'] = "default-src 'self'"
    return response

# Layer 4: SQL Parameterization (psycopg2)
cur.execute("SELECT * FROM blacklist_ips WHERE ip_address = %s", (ip,))
{{/code}}

{{success}}
**✅ 보안 테스트 완료**
* CSRF 공격 방어 ✅
* SQL Injection 방어 ✅
* Rate Limiting 동작 ✅
* 테스트 위치: {{code}}tests/security/test_security.py{{/code}}
{{/success}}

---

== 7️⃣ 네트워크 아키텍처 ==

=== 🌐 네트워크 구성 ===

{{panel title="**🔌 Docker Networks**"}}
* **blacklist-network**: 내부 통신 (Bridge)
* **traefik-public**: 외부 접근 (Traefik 연동, 외부 네트워크)
{{/panel}}

(% class="wikitable" %)
|=(% style="background:#607D8B; color:white;" %)컨테이너|=(% style="background:#607D8B; color:white;" %)blacklist-network|=(% style="background:#607D8B; color:white;" %)traefik-public
|blacklist-frontend|✅|✅
|blacklist-app|✅|❌
|blacklist-collector|✅|❌
|blacklist-postgres|✅|❌
|blacklist-redis|✅|❌

{{info}}
**🔐 보안 격리**
* Frontend만 외부 접근 가능 (Traefik 통해)
* 나머지 서비스는 내부망 격리
* PostgreSQL, Redis는 외부 노출 안 됨
{{/info}}

---

**다음 섹션**: [[3. API 사용 가이드>>doc:blacklist-platform.03-api]]
